import firebase from 'firebase/app';
import 'firebase/database'; // Import specific Firebase services if needed


function Admin() {
    return (
        <h2>Admin Page</h2>
    );
}

export default Admin;